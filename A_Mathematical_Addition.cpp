#include<bits/stdc++.h>
using namespace std;
#define ll long long
void solve(){
    ll u,v;
    cin >> u >> v;
    ll x = -u*u;
    ll y = v*v;
    cout << x << " " << y << "\n";
 
}
signed main(){
    ios_base::sync_with_stdio(false);
    cin.tie(0);
    cout.tie(0);
    int t;
    cin >> t;
    while(t--){
        solve();
    }
    return 0;
}